QNX port of the ART 6.0.1 Quick compiler (q20-android research)

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/utils/assembler.cc	2026-09-23 07:47:04
+++ /Users/piotrpestka/github/q20-android/art/compiler/utils/assembler.cc	2026-09-23 17:53:53
@@ -21,11 +21,13 @@
 
 #include "arm/assembler_arm32.h"
 #include "arm/assembler_thumb2.h"
+#if !defined(__QNXNTO__)
 #include "arm64/assembler_arm64.h"
 #include "mips/assembler_mips.h"
 #include "mips64/assembler_mips64.h"
 #include "x86/assembler_x86.h"
 #include "x86_64/assembler_x86_64.h"
+#endif
 #include "globals.h"
 #include "memory_region.h"
 
@@ -115,6 +117,7 @@
       return new arm::Arm32Assembler();
     case kThumb2:
       return new arm::Thumb2Assembler();
+#if !defined(__QNXNTO__)
     case kArm64:
       return new arm64::Arm64Assembler();
     case kMips:
@@ -125,6 +128,7 @@
       return new x86::X86Assembler();
     case kX86_64:
       return new x86_64::X86_64Assembler();
+#endif
     default:
       LOG(FATAL) << "Unknown InstructionSet: " << instruction_set;
       return nullptr;

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/trampolines/trampoline_compiler.cc	2026-09-23 07:47:04
+++ /Users/piotrpestka/github/q20-android/art/compiler/trampolines/trampoline_compiler.cc	2026-09-23 17:57:44
@@ -18,11 +18,13 @@
 
 #include "jni_env_ext.h"
 #include "utils/arm/assembler_arm.h"
+#if !defined(__QNXNTO__)
 #include "utils/arm64/assembler_arm64.h"
 #include "utils/mips/assembler_mips.h"
 #include "utils/mips64/assembler_mips64.h"
 #include "utils/x86/assembler_x86.h"
 #include "utils/x86_64/assembler_x86_64.h"
+#endif
 
 #define __ assembler->
 
@@ -55,6 +57,7 @@
 }
 }  // namespace arm
 
+#if !defined(__QNXNTO__)
 namespace arm64 {
 static const std::vector<uint8_t>* CreateTrampoline(EntryPointCallingConvention abi,
                                                     ThreadOffset<8> offset) {
@@ -91,7 +94,9 @@
   return entry_stub.release();
 }
 }  // namespace arm64
+#endif  // !defined(__QNXNTO__)
 
+#if !defined(__QNXNTO__)
 namespace mips {
 static const std::vector<uint8_t>* CreateTrampoline(EntryPointCallingConvention abi,
                                                     ThreadOffset<4> offset) {
@@ -120,7 +125,9 @@
   return entry_stub.release();
 }
 }  // namespace mips
+#endif  // !defined(__QNXNTO__)
 
+#if !defined(__QNXNTO__)
 namespace mips64 {
 static const std::vector<uint8_t>* CreateTrampoline(EntryPointCallingConvention abi,
                                                     ThreadOffset<8> offset) {
@@ -148,8 +155,10 @@
 
   return entry_stub.release();
 }
-}  // namespace mips64
+}  // namespace mips
+#endif  // !defined(__QNXNTO__)
 
+#if !defined(__QNXNTO__)
 namespace x86 {
 static const std::vector<uint8_t>* CreateTrampoline(ThreadOffset<4> offset) {
   std::unique_ptr<X86Assembler> assembler(static_cast<X86Assembler*>(Assembler::Create(kX86)));
@@ -166,7 +175,9 @@
   return entry_stub.release();
 }
 }  // namespace x86
+#endif  // !defined(__QNXNTO__)
 
+#if !defined(__QNXNTO__)
 namespace x86_64 {
 static const std::vector<uint8_t>* CreateTrampoline(ThreadOffset<8> offset) {
   std::unique_ptr<x86_64::X86_64Assembler>
@@ -183,17 +194,20 @@
 
   return entry_stub.release();
 }
-}  // namespace x86_64
+}  // namespace x86
+#endif  // !defined(__QNXNTO__)
 
 const std::vector<uint8_t>* CreateTrampoline64(InstructionSet isa, EntryPointCallingConvention abi,
                                                ThreadOffset<8> offset) {
   switch (isa) {
+#if !defined(__QNXNTO__)
     case kArm64:
       return arm64::CreateTrampoline(abi, offset);
     case kMips64:
       return mips64::CreateTrampoline(abi, offset);
     case kX86_64:
       return x86_64::CreateTrampoline(offset);
+#endif
     default:
       LOG(FATAL) << "Unexpected InstructionSet: " << isa;
       UNREACHABLE();
@@ -206,10 +220,12 @@
     case kArm:
     case kThumb2:
       return arm::CreateTrampoline(abi, offset);
+#if !defined(__QNXNTO__)
     case kMips:
       return mips::CreateTrampoline(abi, offset);
     case kX86:
       return x86::CreateTrampoline(offset);
+#endif
     default:
       LOG(FATAL) << "Unexpected InstructionSet: " << isa;
       UNREACHABLE();

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/dex/quick/quick_compiler.cc	2026-09-23 07:47:04
+++ /Users/piotrpestka/github/q20-android/art/compiler/dex/quick/quick_compiler.cc	2026-09-23 18:03:53
@@ -804,6 +804,7 @@
     case kThumb2:
       mir_to_lir = ArmCodeGenerator(cu, cu->mir_graph.get(), &cu->arena);
       break;
+#if !defined(__QNXNTO__)
     case kArm64:
       mir_to_lir = Arm64CodeGenerator(cu, cu->mir_graph.get(), &cu->arena);
       break;
@@ -817,6 +818,7 @@
     case kX86_64:
       mir_to_lir = X86CodeGenerator(cu, cu->mir_graph.get(), &cu->arena);
       break;
+#endif
     default:
       LOG(FATAL) << "Unexpected instruction set: " << cu->instruction_set;
   }

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/jni/quick/calling_convention.cc	2026-09-23 07:47:04
+++ /Users/piotrpestka/github/q20-android/art/compiler/jni/quick/calling_convention.cc	2026-09-23 18:08:03
@@ -34,6 +34,7 @@
     case kArm:
     case kThumb2:
       return new arm::ArmManagedRuntimeCallingConvention(is_static, is_synchronized, shorty);
+#if !defined(__QNXNTO__)
     case kArm64:
       return new arm64::Arm64ManagedRuntimeCallingConvention(is_static, is_synchronized, shorty);
     case kMips:
@@ -44,6 +45,7 @@
       return new x86::X86ManagedRuntimeCallingConvention(is_static, is_synchronized, shorty);
     case kX86_64:
       return new x86_64::X86_64ManagedRuntimeCallingConvention(is_static, is_synchronized, shorty);
+#endif
     default:
       LOG(FATAL) << "Unknown InstructionSet: " << instruction_set;
       return nullptr;
@@ -109,6 +111,7 @@
     case kArm:
     case kThumb2:
       return new arm::ArmJniCallingConvention(is_static, is_synchronized, shorty);
+#if !defined(__QNXNTO__)
     case kArm64:
       return new arm64::Arm64JniCallingConvention(is_static, is_synchronized, shorty);
     case kMips:
@@ -119,6 +122,7 @@
       return new x86::X86JniCallingConvention(is_static, is_synchronized, shorty);
     case kX86_64:
       return new x86_64::X86_64JniCallingConvention(is_static, is_synchronized, shorty);
+#endif
     default:
       LOG(FATAL) << "Unknown InstructionSet: " << instruction_set;
       return nullptr;

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/linker/relative_patcher.cc	2026-09-23 07:47:04
+++ /Users/piotrpestka/github/q20-android/art/compiler/linker/relative_patcher.cc	2026-09-23 17:58:30
@@ -65,17 +65,19 @@
   };
 
   switch (instruction_set) {
+#if !defined(__QNXNTO__)
     case kX86:
       return std::unique_ptr<RelativePatcher>(new X86RelativePatcher());
     case kX86_64:
       return std::unique_ptr<RelativePatcher>(new X86_64RelativePatcher());
+    case kArm64:
+      return std::unique_ptr<RelativePatcher>(
+          new Arm64RelativePatcher(provider, features->AsArm64InstructionSetFeatures()));
+#endif
     case kArm:
       // Fall through: we generate Thumb2 code for "arm".
     case kThumb2:
       return std::unique_ptr<RelativePatcher>(new Thumb2RelativePatcher(provider));
-    case kArm64:
-      return std::unique_ptr<RelativePatcher>(
-          new Arm64RelativePatcher(provider, features->AsArm64InstructionSetFeatures()));
     default:
       return std::unique_ptr<RelativePatcher>(new RelativePatcherNone);
   }

--- /var/folders/03/bdcsj20x1q372qfxl9pbffz40000gn/T/opencode/art-pristine/compiler/compiler.cc	2026-09-23 07:47:03
+++ /Users/piotrpestka/github/q20-android/art/compiler/compiler.cc	2026-09-23 18:07:22
@@ -30,7 +30,12 @@
       return CreateQuickCompiler(driver);
 
     case kOptimizing:
+#if !defined(__QNXNTO__)
       return CreateOptimizingCompiler(driver);
+#else
+      LOG(FATAL) << "Optimizing compiler not built for QNX";
+      UNREACHABLE();
+#endif
 
     default:
       LOG(FATAL) << "UNREACHABLE";
